apiVersion: apps/v1
kind: Deployment
metadata:
  name: ozon-bot
spec:
  template:
    spec:
      containers:
        - name: bot
          resources:
            requests:
              memory: "512Mi"
              cpu: "500m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
